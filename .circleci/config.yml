orbs:
  win: circleci/windows@1.0.0

jobs:
  build:
    executor: machine
    steps:
      - checkout
      - run: 'g++ main.cpp'

workflows:
  version: 2
    build_and_test: 
                jobs:
		        - build
			- test:
			          requires:
				       - build
	
